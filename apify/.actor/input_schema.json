{
  "title": "StatCite query",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "operation": {
      "title": "Operation",
      "type": "string",
      "description": "What to do: indicator (get values), verify (check a claimed figure), snapshot (country overview), inflation (adjust across years), fx (convert currencies), series (raw series id), search, registry (list indicators).",
      "enum": ["indicator", "verify", "snapshot", "inflation", "fx", "series", "search", "registry"],
      "default": "indicator"
    },
    "indicator": {
      "title": "Indicator key",
      "type": "string",
      "description": "e.g. inflation_cpi, gdp_growth, unemployment_rate, govt_debt_gdp (run operation=registry for the full list).",
      "editor": "textfield",
      "prefill": "inflation_cpi"
    },
    "country": {
      "title": "Country",
      "type": "string",
      "description": "ISO3/ISO2 code or English name, e.g. USA, Barbados, Japan.",
      "editor": "textfield",
      "prefill": "USA"
    },
    "period": { "title": "Period (verify)", "type": "string", "description": "Year of the claim, e.g. 2024.", "editor": "textfield" },
    "claimed_value": { "title": "Claimed value (verify)", "type": "string", "description": "The figure to check, in the series' own units.", "editor": "textfield" },
    "start_year": { "title": "Start year", "type": "integer", "description": "Optional filter." },
    "end_year": { "title": "End year", "type": "integer", "description": "Optional filter." },
    "latest_only": { "title": "Latest value only", "type": "boolean", "description": "Return just the most recent published observation.", "default": false },
    "transform": { "title": "Transform", "type": "string", "enum": ["none", "yoy", "pct_change", "index"], "default": "none", "description": "Computed transform (with method note)." },
    "series_id": { "title": "Series id (series)", "type": "string", "description": "worldbank/CODE, fred/ID, or dbnomics/PROVIDER/DATASET/SERIES.", "editor": "textfield" },
    "amount": { "title": "Amount (inflation / fx)", "type": "string", "description": "Numeric amount.", "editor": "textfield" },
    "from_year": { "title": "From year (inflation)", "type": "integer" },
    "to_year": { "title": "To year (inflation)", "type": "integer" },
    "from": { "title": "From currency (fx)", "type": "string", "description": "ISO 4217, e.g. USD.", "editor": "textfield" },
    "to": { "title": "To currency (fx)", "type": "string", "description": "ISO 4217, e.g. BBD.", "editor": "textfield" },
    "date": { "title": "Date (fx)", "type": "string", "description": "YYYY-MM-DD (daily) or YYYY (annual average).", "editor": "textfield" },
    "query": { "title": "Query (search)", "type": "string", "editor": "textfield" },
    "items": {
      "title": "Batch items",
      "type": "array",
      "description": "Optional: array of parameter objects to run in one call (each may set its own operation). One pay-per-event charge per successful item.",
      "editor": "json"
    }
  },
  "required": ["operation"]
}
